name: Package and Release Skill

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest

    # The repo's default workflow token is read-only, so creating a release
    # returned 403 and every release run failed (v1.0.0, v1.1.0, v2.0.0).
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Package the skill
        run: |
          cd skill
          python package_skill.py fs25-modding-skill/
          ls -la *.skill
      
      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: FS25 Claude Skill ${{ steps.version.outputs.VERSION }}
          body_path: CHANGELOG.md
          files: |
            skill/fs25-modding-skill.skill
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
